Dataset Description - Student Performance ML Dataset

The dataset student_performance_ml.csv contains academic and behavioral information of students. 
The objective of this dataset is to predict whether a student will Pass(1) or Fail(0) based on various input features.

Each row in the dataset represents one student and each column represents a measurable factor that may influence academic performnance.

Features Description:-
    1) StudyHours - Number of hours a student studies per day.
    2) Attendance - Percentage of class attendance.
    3) PreviousScore - Marks obtained in the previous examination.
    4) AssignmentsCompleted - Number of assignments completed by the student.
    5) SleepHours - Average number of hours the student sleeps per day.
    6) FinalResult - Target variable (Output):
        A. 1 --> Pass
        B. 0 --> Fail

Objective of the Dataset
The goal is to:
    1) Analyze how different factors affect student performance.
    2) Build a Machine Learning model to predict whether a student will pass or fail.
    3) Understand concepts such as training, testing, accuracy, confusion matrix, overfitting and model evaluation.

1. Import DecisionTreeClassifier from sklearn.
Create a model object and train it using fit().

2. Use the trained model to predict results for X_test.
Display predicted values along with actual values.

3. Calculate model accuracy using accuracy_score.
Display the result in percentage format.

4. Generate confusion matrix using sklearn.
Display it using confusionMatrixDisplay.
Explain clearly:
    a. True Positive
    b. True Negative
    c. False Positive
    d. False Negative

5. Calculate:
    a. Training accuracy
    b. Testing accuracy
Compare both and comment whether the model is overfitting or underfitting.

6. Train three Decision Tree Models with:
    a. max_depth = 1
    b. max_depth = 3
    c. max_depth = None
Compare their testing accuracies and write your observations.

7. Use the trained model to predict result for a student with:
    a. StudyHours = 6
    b. Attendance = 85
    c. PreviousScore = 66
    d. AssignmentsCompleted = 7
    e. SleepHours = 7
Will the student Pass or Fail?

8. Write a single structured Python program that performs:
    a. Dataset loading
    b. Data analysis
    c. Visualization
    d. Train-test split
    e. Model training
    f. Prediction
    g. Accuracy calculation
    h. Confusion matrix generation
    i. Final conclusion