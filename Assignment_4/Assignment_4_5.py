# Predict the output

s = "Python"
print(id(s))

s = s + "3"
print(id(s))

# the memory address will be different even if they are immuable because the data is not same so it won't share the memory